## [0.0.1] - 2026-04-27

- Initial release.
- `SkyCommands` client with `register`, `unregister`, `heartbeat`, `setMetadata`, `initialize`, and `runner`.
- Built-in `ping` command handled internally.
