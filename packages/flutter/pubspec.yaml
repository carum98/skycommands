name: 'skycommands'
description: 'SkyCommands Flutter is an adapter for [SkyCommands]. It provides a way to use SkyCommands in your Flutter application.'
version: 0.0.1

platforms:
  android:
  ios:

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: 16.0.4

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  